<p id="notice"><%= notice %></p>

<h1>Apartments</h2>
<p>with features <%= @sherlock.features.join(', ') %></p>

<% @apartments.each do |apartment| %>
	<p style="font-weight: bold;"><%= link_to apartment.title, apartment.url %></p>
	<p>Features: <%= apartment.features.join(', ') %></p>
	<hr />
<% end %>


<%= link_to 'Edit', edit_sherlock_path(@sherlock) %> |
<%= link_to 'Back', sherlocks_path %>
